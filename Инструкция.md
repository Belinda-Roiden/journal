# 📓 Электронный журнал

**Инструкция для старосты**  
**Версия:** 1.0  
**Дата:** Февраль 2026

---

## 1) Что это такое

`Электронный журнал` — это файл формата `.html`, который открывается в любом браузере на телефоне или компьютере.

С его помощью можно:
- вести учёт посещаемости по всем предметам;
- фиксировать пропуски в часах;
- смотреть профиль и статистику по каждому студенту.

Данные сохраняются в облаке (**JSONBin**) и доступны с любого устройства в реальном времени.

---

## 2) Первый запуск и настройка

### Шаг 1. Создать хранилище в JSONBin

**JSONBin** — бесплатный сервис для хранения данных журнала в интернете.

1. Открой `jsonbin.io` и зарегистрируйся (можно через Google).
2. Перейди в раздел **API Keys** и скопируй **Master Key** (длинная строка, обычно начинается с `$2a$...`).
3. Перейди в раздел **Bins** и нажми **Create Bin**.
4. В поле содержимого введи:

```json
{"ready": true}
```

5. Нажми **Save Bin**.
6. Скопируй **Bin ID** (из адресной строки или карточки bin).

> ⚠️ **Важно:** храни `API-ключ` и `Bin ID` в надёжном месте. Они нужны при входе с нового устройства.

### Шаг 2. Подключить хранилище к журналу

При первом открытии файла `journal.html` введи:
- `Bin ID`;
- `API-ключ`.

Нажми **«Подключить и продолжить»**.

Если данные введены верно, откроется создание аккаунта.

### Шаг 3. Создать аккаунт старосты

Заполни:
- **ФИО** (вводи одинаково каждый раз);
- **номер группы**;
- **пароль** (минимум 4 символа) и подтверждение.

Нажми **«Создать аккаунт»**.

После этого появится **резервный код**.

> ⚠️ **Резервный код показывается только один раз.**  
> Сразу сохрани его в заметках или записной книжке.

### Шаг 4. Повторный вход

При следующем открытии журнала нужно ввести только:
- ФИО;
- пароль.

Если открываешь журнал на **новом устройстве**, снова потребуются `Bin ID` и `API-ключ`, а затем регистрация аккаунта на этом устройстве.

> ✅ Данные журнала при этом не теряются — они подгружаются из облака.

---

## 3) Главный экран

На главном экране отображается список предметов:
- название предмета;
- ФИО преподавателя;
- часы по предмету.

### Что можно делать

- Редактировать название предмета — нажми на текст.
- Редактировать ФИО преподавателя — нажми под названием предмета.
- Открыть журнал предмета — кнопка **«Открыть журнал →»**.
- Удалить предмет — кнопка `×` в правом углу карточки.
- Добавить предмет — кнопка **«+ Добавить предмет»**.
- Перейти к списку студентов — кнопка `👤` в шапке.
- Выйти из аккаунта — кнопка **«Выйти»**.

Под именем в шапке отображается статус сохранения:
- **`✓ Сохранено`** — все изменения уже в облаке.

> ⚠️ Перезагружай страницу только после статуса **`✓ Сохранено`**.

---

## 4) Список студентов

Открывается по кнопке `👤` в шапке главного экрана.

### Доступные действия

- Редактировать имя студента — нажми на имя.
- Добавить студента — **«+ Добавить студента»**.
- Удалить студента — `×` рядом с именем (с подтверждением).
- Открыть профиль — **«Профиль →»**.

Рядом с именем показывается суммарное количество пропущенных часов по всем предметам.

По умолчанию при первом запуске создаётся **10 студентов** (число можно менять).

---

## 5) Профиль студента

Профиль автоматически показывает статистику пропусков на основе посещаемости.

### Что видно в профиле

- Сводка часов по причинам:
  - болезнь;
  - уважительная причина;
  - без причины.
- Детализация по каждому предмету.

Предметы, где ещё не было занятий, в профиле не отображаются.

---

## 6) Журнал предмета и занятия

### Журнал предмета

Нажми **«Открыть журнал →»** на карточке предмета.

Отображается список занятий:
- дата;
- тема;
- тип;
- часы;
- краткая статистика посещаемости.

Занятия идут в обратном порядке: новое — сверху.

Действия:
- **«+ Добавить занятие»** — создать занятие;
- нажать на карточку занятия — открыть и редактировать.

### Карточка занятия

В карточке можно заполнить:
- дату;
- тему;
- тип занятия (`Лекция`, `Практика`, `Семинар`, `Лабораторная`);
- количество часов;
- посещаемость каждого студента.

Чтобы сменить статус студента, нажми на цветной кружок рядом с именем — статус меняется по кругу.

Также доступны кнопки:
- **«Все присутствуют»**;
- **«Все отсутствуют»**.

---

## 7) Статусы посещаемости

| Код | Статус | Описание |
|---|---|---|
| `П` | Присутствует | Студент был на занятии |
| `Б` | Болезнь | Пропуск по болезни (со справкой/больничным) |
| `У` | Уваж. причина | Пропуск по уважительной причине |
| `Н` | Отсутствует | Пропуск без уважительной причины |

Порядок переключения статусов:

`П → Б → У → Н → П → ...`

---

## 8) Если забыла пароль

На экране входа нажми **«Забыла пароль»** и введи:
1. резервный код;
2. номер группы (точно как при регистрации);
3. новый пароль и подтверждение.

После сброса создаётся **новый резервный код**.

> ⚠️ Старый резервный код сразу перестаёт работать.

> ℹ️ На каждом устройстве — свой аккаунт и свой резервный код. Подписывай коды, чтобы не перепутать.

---

## 9) Как открыть журнал на новом устройстве

1. Скопируй файл `journal.html` на новое устройство.
2. Открой его в браузере.
3. Введи свой `Bin ID` и `API-ключ` от JSONBin.
4. Зарегистрируйся на этом устройстве (ФИО, группа, пароль).
5. Данные журнала автоматически загрузятся из облака.

Если нужно сбросить аккаунт на текущем устройстве (например, для повторной регистрации), используй кнопку:

**«Сменить устройство / сбросить аккаунт»** на экране входа.

---

## 10) Полезные советы

- Закрывай или перезагружай страницу только после **`✓ Сохранено`**.
- Храни `резервный код`, `API-ключ` и `Bin ID` в надёжном месте.
- Один файл `journal.html` = один журнал.
- Не передавай свой файл другим старостам: они получат доступ к твоим данным.
- Для новых занятий статус `Н` (без причины) выставляется по умолчанию — при необходимости поменяй вручную.
- Профили студентов обновляются автоматически после любого изменения посещаемости.

---

**Электронный журнал · ПР-23-1б-2**
